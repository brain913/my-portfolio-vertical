import React from 'react';

const UIComponents = () => {
    return (
        <div>
            {/* Reusable UI elements like buttons or cards can be added here */}
        </div>
    );
};

export default UIComponents;